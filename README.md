# Personal Portfolio — R C Vishal

This is a simple static portfolio scaffold (HTML/CSS/JS).

To preview locally, open `index.html` in your browser or run a static server, for example:

```bash
# using Python 3
python -m http.server 8000
# then open http://localhost:8000
```

Files created:
- `index.html`
- `styles.css`
- `script.js`

If you want changes (different color, additional projects, contact form, downloadable resume), tell me what to update.